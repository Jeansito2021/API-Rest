# Configuraci�n de la base de datos H2 (base de datos en memoria)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true

# Configuraci�n de la API
server.port=8080

# Habilitar el log de las queries de JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuraci�n de la ejecuci�n as�ncrona de tareas (Event-Driven)
spring.task.execution.pool.size=10
spring.task.execution.pool.keep-alive=60s

# Configuraci�n de logging (opcional, para depuraci�n)
logging.level.org.springframework=INFO
logging.level.com.example.demo=DEBUG

# Configuraci�n de RabbitMQ
# spring.rabbitmq.host=localhost
# spring.rabbitmq.port=5672

